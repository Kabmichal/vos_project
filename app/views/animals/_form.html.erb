<%= form_for(@animals) do |f| %>
  <%= render 'shared/error_messages', object: @animals %>

  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.submit "Create animal", class: "btn btn-primary" %>
<% end %>