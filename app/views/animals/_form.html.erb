<%= form_for(@animal) do |f| %>
  <%= render 'shared/error_messages', object: @animal %>

  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.submit "Sign up", class: "btn btn-primary" %>
<% end %>