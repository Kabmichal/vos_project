<% provide(:title, @animal.name) %>
<h1><%= @animal.name %></h1>
<div class="row">
  <aside class="col-md-4">

    <section class="user_info">
      <%= render 'users/all_users' %>
    </section>
    <section class="animal_foods">
      <%= render 'animals/animal_food_form' %>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @animal.animal_foods.any? %>
      <h3>Animal was feed <%= @animal.animal_foods.count %> times</h3>
      <ol class="zviera">
        <%= render @animal_foods %>
      </ol>
      <%= will_paginate @animal_foods %>
    <% end %>
  </div>
</div>